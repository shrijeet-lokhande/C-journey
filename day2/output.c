#include<stdio.h>
int main(){
    printf("This program covers the practice of the outputs in C");
    //printing a integer, so we need to use the %f specifier as the 
    //placeholder

    printf("this is an integer %d", 4);

    /*so while printing the resp data type,
    we need to have the res format specfier*/

    return 0;
}